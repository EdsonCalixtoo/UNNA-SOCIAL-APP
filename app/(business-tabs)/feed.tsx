import FeedScreen from '../(tabs)/index';

// Reutilizamos a mesma tela do Feed (Explorar) para as Empresas,
// permitindo que elas vejam, curtam e interajam com eventos.
export default function BusinessFeed() {
  return <FeedScreen />;
}
